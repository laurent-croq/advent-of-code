#!/usr/bin/python3.8

import aoc

def puzzles(input_lines, **extra_args):
    #for n in [ int(n) for n in input_lines ]
    #for line in input_lines:
    
    yield(None)
    yield(None)

aoc.run(puzzles)
